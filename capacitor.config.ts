import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aman.medical.management',
  appName: 'نظام أمان الطبي',
  webDir: 'dist'
};

export default config;
